apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: application-hub
  namespace: app-hub
  annotations:
    kubernetes.io/ingress.class: apisix
    k8s.apisix.apache.org/http-to-https: "true"
    apisix.ingress.kubernetes.io/use-regex: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-http01-apisix"
    k8s.apisix.apache.org/enable-cors: "true"
    k8s.apisix.apache.org/upstream-read-timeout: "600s"
    k8s.apisix.apache.org/enable-websocket: "true"
    argocd.argoproj.io/ignore-healthcheck: "true"
spec:
  ingressClassName: apisix
  rules:
    - host: applicationhub.develop-v2.eoepca.org
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: application-hub-proxy-public
                port:
                  number: 80
  tls:
    - hosts:
        - applicationhub.develop-v2.eoepca.org
      secretName: applicationhub-tls
