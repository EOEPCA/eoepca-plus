ingress:
  enabled: true
  className: apisix
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-http01-apisix
    apisix.ingress.kubernetes.io/use-regex: "true"
    k8s.apisix.apache.org/upstream-read-timeout: "600s"
    k8s.apisix.apache.org/enable-cors: "true"
    k8s.apisix.apache.org/http-to-https: "true"
    argocd.argoproj.io/ignore-healthcheck: "true"
  hosts:
    - host: datacube-access.rke2.deploybox.co.uk
      paths:
        - path: /
          pathType: Prefix
  tls:
  - hosts:
      - datacube-access.rke2.deploybox.co.uk
    secretName: datacube-access-tls
resources:
  requests:
    memory: "256Mi"
    cpu: "250m"
  limits:
    memory: "512Mi"
    cpu: "500m"
config:
  dataBackend: https://eoapi.rke2.deploybox.co.uk/stac/
