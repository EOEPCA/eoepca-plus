# Note: This file is not needed if OPAL is deployed via the iam-bb Helm chart,
# because the chart is able to create the route automatically.
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: opa-route
spec:
  http:
  - name: opa
    match:
      hosts:
      - iam-opa.rke2.deploybox.co.uk
      paths:
      - /*
    backends:
    - serviceName: iam-opa
      servicePort: 8181
    plugins:
      # TODO: Configure client secrets through secretRef
      - name: openid-connect
        enable: true
        config:
          client_id: "opa"
          client_secret: "RkAUBGEzUCCgKBydiagDGqW5JJeXYdDQ"
          #bearer_only: true
          #realm: "eoepca"
          #redirect_uri: "http://iam-test.rke2.deploybox.co.uk/"
          access_token_in_authorization_header: true
          discovery: "https://iam-auth.rke2.deploybox.co.uk/realms/eoepca/.well-known/openid-configuration"
      - name: authz-keycloak
        enable: true
        config:
          client_id: "opa"
          client_secret: "RkAUBGEzUCCgKBydiagDGqW5JJeXYdDQ"
          discovery: "https://iam-auth.rke2.deploybox.co.uk/realms/eoepca/.well-known/uma2-configuration"
          #permissions: "Resource all"
          lazy_load_paths: true
          ssl_verify: false
  - name: opa
    match:
      hosts:
        - iam-opa.rke2.deploybox.co.uk
      paths:
        - /*
    backends:
      - serviceName: iam-opa
        servicePort: 8181
    plugins:
      - name: openid-connect
        enable: true
        config:
          client_id: "opa"
          client_secret: "RkAUBGEzUCCgKBydiagDGqW5JJeXYdDQ"
          access_token_in_authorization_header: true
          discovery: "https://iam-auth.rke2.deploybox.co.uk/realms/eoepca/.well-known/openid-configuration"
      - name: authz-keycloak
        enable: true
        config:
          client_id: "opa"
          client_secret: "RkAUBGEzUCCgKBydiagDGqW5JJeXYdDQ"
          discovery: "https://iam-auth.rke2.deploybox.co.uk/realms/eoepca/.well-known/uma2-configuration"
          #permissions: "Resource all"
          lazy_load_paths: true
          ssl_verify: false
