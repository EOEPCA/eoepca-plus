global:
  storageClass: managed-nfs-storage-retain
gateway:
  tls:
    enabled: true
    containerPort: 443
#ingress:
#  enabled: true
ingress-controller:
  enabled: true
# TODO: Maybe add the following if necessary
#pluginAttrs:
#  redirect:
#    https_port: 443
