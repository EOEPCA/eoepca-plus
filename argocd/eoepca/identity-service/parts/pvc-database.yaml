apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: identity-service
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: managed-nfs-storage-retain
  resources:
    requests:
      storage: 5Gi
