apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: notifications-wildcard-ingress
  namespace: knative-serving
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-dns-prod
    kubernetes.io/ingress.class: apisix
    # apisix never populates status.loadBalancer, so the custom Ingress health
    # check would otherwise leave this stuck on "Progressing" forever.
    argocd.argoproj.io/ignore-healthcheck: "true"
spec:
  ingressClassName: apisix
  rules:
  - host: '*.notifications.develop-v2.eoepca.org'
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: kourier
            port:
              number: 80
  tls:
    - hosts:
      - '*.notifications.develop-v2.eoepca.org'
      secretName: notifications-wildcard-tls
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: notifications-wildcard-tls
  namespace: knative-serving
spec:
  dnsNames:
  - '*.notifications.develop-v2.eoepca.org'
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-dns-prod
  secretName: notifications-wildcard-tls