# Wildcard certificate for preliminary APISIX ingress *.develop-v2.eoepca.org
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apx-all-tls
  namespace: ingress-apisix
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  secretName: apx-all-tls
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  isCA: false
  usages:
    - server auth
    - client auth
  dnsNames:
    - "*.develop-v2.eoepca.org"
  issuerRef:
    name: letsencrypt-dns-prod
    kind: ClusterIssuer
