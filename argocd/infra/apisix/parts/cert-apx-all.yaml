# Wildcard certificate for preliminary APISIX ingress *.rke2.deploybox.co.uk
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apx-all-tls
  namespace: ingress-apisix
  annotations:
    argocd.argoproj.io/sync-wave: "-10"
spec:
  secretName: apx-all-tls
  duration: 2160h # 90d
  renewBefore: 360h # 15d
  isCA: false
  usages:
    - server auth
    - client auth
  dnsNames:
    - "*.rke2.deploybox.co.uk"
  issuerRef:
    # Use the DNS staging issuer while repeated clean-cluster rebuilds would
    # otherwise hit Let's Encrypt production duplicate-certificate limits.
    name: letsencrypt-dns-prod
    kind: ClusterIssuer
