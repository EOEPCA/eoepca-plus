apiVersion: apisix.apache.org/v2
kind: ApisixPluginConfig
metadata:
  name: minio-ingress
spec:
  plugins:
    - name: client-control
      enable: true
      config:
        # matches S3's per-part maximum. 0 was causing 413 errors.
        max_body_size: 5368709120
